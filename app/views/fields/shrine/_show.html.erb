<% if field.url_only? -%>
<%= link_to field.resource.url, field.resource.url if field.resource.url.present? %>
<% else -%>
<%= image_tag field.resource.url if field.resource.url.present? %>
<% end -%>
